
import java.util.Random;
public class ArrayUtil {
    public double getArrayTotal(double[] array) {
        for (int i = 0; i < size; i++) {
            randomarray[i] = 1.0 + (4.0 * random.nextDouble()); // Range: 1.0 to 5.0
        return Random();
    }
}
